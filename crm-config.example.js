/**
 * Скопируйте в crm-config.js и подставьте URL и anon key из Supabase → Settings → API.
 * Файл crm-config.js не коммитьте (см. .gitignore).
 */
window.CRM_CONFIG = {
  supabaseUrl: 'https://YOUR_PROJECT_REF.supabase.co',
  supabaseAnonKey: 'YOUR_SUPABASE_ANON_KEY',
};
