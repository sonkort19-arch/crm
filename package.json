{
  "name": "too-crm",
  "private": true,
  "description": "Статический Too CRM; шрифты подключаются из manrope.css в корне (без npm-зависимостей для рантайма).",
  "scripts": {
    "build": "node scripts/gen-crm-config.mjs"
  }
}
